# Cursor rules bundle — Callback

Governance lives at repo root: `AGENTS.md`, `SPEC.md`, `ROADMAP.md`. Cursor-specific guidance: `agent_rules/cursor.md`. Prefer XML layouts and Views only; no Compose.
